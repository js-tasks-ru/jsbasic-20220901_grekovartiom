function checkSpam(str) {
  if (str.includes ("1XbeT now") ) {
    return true;
  } else if
  (str.includes ("free xxxxx") ) {
    return true;
  } else if
  (str.includes ("innocent rabbit") ) {
    return false;
  }
    
}
